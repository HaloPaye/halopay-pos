### Summary
Merchants lose connection often. We need to queue transactions in IndexedDB and flush them when back online.

### Acceptance Criteria
- [ ] Implement local database using `idb`.
- [ ] Queue failed POST requests.
- [ ] Add background sync listener to flush queue.

### Tech Stack
Next.js 14 (App Router), React, and IndexedDB.